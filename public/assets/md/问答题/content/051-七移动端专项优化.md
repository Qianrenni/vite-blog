### 七、移动端专项优化

- 使用响应式图片（`srcset` / `picture`）
- 避免 300ms 点击延迟（FastClick 或 `touch-action: manipulation`）
- 减少重定向和重试（节省电量与流量）
- 使用轻量级库（如 Preact 替代 React，Zepto 替代 jQuery）
- 启用硬件加速（`translateZ(0)` / `will-change`）
- 避免 fixed 布局滚动卡顿（改用 absolute + JS 滚动监听）

---