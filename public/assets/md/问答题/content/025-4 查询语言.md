### 4. **查询语言**

| **SQL** | **NoSQL** |
|--------|----------|
| 使用标准化的 **SQL（Structured Query Language）**，语法统一，功能强大（JOIN、子查询、聚合等）。 | **无统一查询语言**，各数据库有自己 API 或查询语法（如 MongoDB 的 BSON 查询、Cassandra 的 CQL）。<br>通常**不支持 JOIN**（需应用层处理关联）。 |

---