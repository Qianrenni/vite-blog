### 10. 其他常见攻击

- **点击劫持（Clickjacking）**：通过透明 iframe 诱骗用户点击。
    - 防御：设置 `X-Frame-Options` 或 `Content-Security-Policy: frame-ancestors`。
- **HTTP 请求走私（HTTP Request Smuggling）**：利用代理/服务器对请求解析不一致，绕过安全控制。
- **业务逻辑漏洞**：如优惠券重复使用、积分刷取等，依赖人工审计发现。