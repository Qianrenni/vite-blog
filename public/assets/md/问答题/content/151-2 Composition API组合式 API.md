### 2. **Composition API（组合式 API）**
- **Vue 2**：主要使用 Options API（data、methods、computed 等选项组织逻辑），在大型组件中逻辑容易分散。
- **Vue 3**：引入 **Composition API**（如 `setup()`、`ref`、`reactive`、`computed`、`watch` 等），允许按功能组织逻辑，提升代码复用性和可维护性。同时仍支持 Options API。

---