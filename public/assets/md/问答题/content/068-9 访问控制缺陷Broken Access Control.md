### 9. 访问控制缺陷（Broken Access Control）

- **原理**：未正确验证用户权限，导致越权访问（如普通用户访问管理员接口）。
- **类型**：
    - 水平越权（访问同级用户数据）；
    - 垂直越权（普通用户执行管理员操作）。
- **防御**：
    - 每次请求都验证权限；
    - 使用基于角色的访问控制（RBAC）；
    - 避免依赖前端隐藏按钮实现权限控制。

---