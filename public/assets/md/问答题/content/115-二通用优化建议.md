### 二、通用优化建议

| 问题类型 | 优化措施 |
|--------|--------|
| 函数/表达式 | 改写查询，避免对索引列操作 |
| 类型不一致 | 确保应用层传参类型匹配 |
| 复合索引 | 遵循最左前缀，合理设计顺序 |
| `LIKE '%xxx'` | 避免前导通配符，考虑全文索引 |
| `OR` 条件 | 确保所有列有索引，或拆为 `UNION` |
| 低选择性 | 删除无效索引，避免干扰优化器 |
| 回表过多 | 使用覆盖索引 |
| 执行计划异常 | 使用 `EXPLAIN` / `EXPLAIN FORMAT=JSON` 分析 |

---