### 🔒 四、关于端口与访问控制的澄清

- 后端应**绑定 `127.0.0.1:8080`**（而非 `0.0.0.0`），只允许本机访问。
- Nginx 监听 `0.0.0.0:80`（对外服务），并通过 `proxy_pass http://127.0.0.1:8080` 访问后端。
- **不要用防火墙阻止 `127.0.0.1` 访问本机端口**，否则 Nginx 代理会失败。
- 这种架构既**规避跨域**，又**提升安全性**（后端不可被直连）。

---