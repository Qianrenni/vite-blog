### 九、安全与性能兼顾

- CSP（内容安全策略）避免内联脚本阻塞解析
- 避免使用 `eval()` / `new Function()`（影响优化和安全）
- 使用 Subresource Integrity（SRI）保证 CDN 资源完整性

> **“减数量、压体积、用缓存、提并发、优渲染、控脚本、重体验、常监控”**


前端性能优化没有“银弹”，需结合项目实际、用户设备、网络环境综合施策。建议从“核心指标监控”入手，优先优化影响用户体验最严重的瓶颈。