### 7. **生命周期钩子变化**
- Composition API 中生命周期钩子前加 `on` 前缀，如：
  - `beforeCreate` / `created` → 在 `setup()` 中直接执行
  - `mounted` → `onMounted()`
  - `beforeDestroy` → `onBeforeUnmount()`
  - `destroyed` → `onUnmounted()`

---