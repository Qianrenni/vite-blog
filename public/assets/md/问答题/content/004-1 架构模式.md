### 1. **架构模式**

- **传统模式（耦合式开发）**  
  前端（HTML、CSS、JS）和后端（如 Java、PHP、Python）紧密耦合。后端不仅处理业务逻辑和数据，还负责生成完整的 HTML 页面（如使用
  JSP、Thymeleaf、PHP 模板等），直接渲染给浏览器。

- **前后端分离**  
  前端和后端完全解耦。后端只提供 API（通常是 RESTful API 或 GraphQL），返回 JSON/XML 数据；前端（如 React、Vue、Angular）通过
  AJAX/Fetch 调用这些接口，自行渲染页面。

---