### ✅ 四、实用建议（针对你的场景）
- 字符串较短（如 `'abc'`），用**多项式哈希 + base=31 + size=101** 是最佳实践。
- 如果**必须用 size=100**，则选 **base=31 或 37**（与 100 互质）。
- 避免使用语言内置 `hash()` 做持久化存储（Python 中其值在不同运行间可能变化）。
- 对安全性要求高（如防碰撞攻击）？用 **SHA256(s) % size**，但性能较低。

---