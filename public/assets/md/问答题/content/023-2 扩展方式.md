### 2. **扩展方式**

| **SQL** | **NoSQL** |
|--------|----------|
| 主要通过**垂直扩展**（提升单机性能：CPU、内存、磁盘）来应对负载增长。 | 天然支持**水平扩展**（增加服务器节点），适合大规模分布式部署。 |

> 虽然部分现代 SQL 数据库（如 CockroachDB、TiDB）也支持水平扩展，但传统关系型数据库（如 MySQL、PostgreSQL）扩展性有限。

---