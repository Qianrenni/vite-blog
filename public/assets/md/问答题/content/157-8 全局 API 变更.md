### 8. **全局 API 变更**
- Vue 2 的全局 API（如 `Vue.component()`、`Vue.directive()`）在 Vue 3 中改为 **应用实例方法**：
  ```js
  // Vue 2
  Vue.component('my-comp', MyComp)

  // Vue 3
  const app = createApp(App)
  app.component('my-comp', MyComp)
  ```

---