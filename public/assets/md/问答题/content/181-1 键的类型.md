### 1. **键的类型**

| 类型       | 键的类型                     |
|------------|------------------------------|
| `Object`   | 只能是 **字符串** 或 **Symbol** |
| `Map`      | **任意类型**（包括对象、函数等） |
| `WeakMap`  | **只能是对象**（不能是原始值） |

---