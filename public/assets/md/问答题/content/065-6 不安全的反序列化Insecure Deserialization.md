### 6. 不安全的反序列化（Insecure Deserialization）

- **原理**：应用程序反序列化不可信数据，导致远程代码执行（RCE）或逻辑绕过。
- **常见于**：Java、PHP、Python 等支持对象序列化的语言。
- **防御**：
    - 避免反序列化用户输入；
    - 使用数字签名验证序列化数据完整性；
    - 采用安全的替代方案（如 JSON）。

---