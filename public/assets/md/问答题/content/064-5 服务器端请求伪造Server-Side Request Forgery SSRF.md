### 5. 服务器端请求伪造（Server-Side Request Forgery, SSRF）

- **原理**：攻击者诱使服务器向内部或外部系统发起请求（如访问内网服务、云元数据接口）。
- **危害**：探测内网、读取敏感信息（如 AWS IAM 角色凭证）、攻击内部系统。
- **防御**：
    - 禁止用户控制 URL；
    - 使用白名单限制可访问的域名/IP；
    - 禁用不必要的协议（如 file://, gopher://）。

---