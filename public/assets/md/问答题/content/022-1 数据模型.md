### 1. **数据模型**

| **SQL** | **NoSQL** |
|--------|----------|
| 采用**关系模型**，数据以**表（Table）**形式组织，行（记录）和列（字段）结构固定。 | 采用**灵活的数据模型**，常见类型包括：<br>• **文档型**（如 MongoDB）<br>• **键值型**（如 Redis）<br>• **列族型**（如 Cassandra）<br>• **图数据库**（如 Neo4j） |

> SQL：结构严格（Schema-on-write）<br>NoSQL：结构灵活（Schema-on-read）

---