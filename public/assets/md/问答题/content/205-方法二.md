### 方法二
```javaScript
// 为所有数组添加 sum 方法
Array.prototype.sum = function() {
    return this.reduce((acc, num) => acc + num, 0);
};

let numbers = [1, 2, 3, 4];
console.log(numbers.sum()); // 输出: 10

// 其他数组也能使用
let otherArray = [5, 6, 7];
console.log(otherArray.sum()); // 输出: 18
```